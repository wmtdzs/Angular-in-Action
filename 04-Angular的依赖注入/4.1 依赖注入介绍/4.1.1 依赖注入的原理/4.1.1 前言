依赖注入，从字面上说，它分为两个部分，一个是依赖，一个是注入。
当一个对象建立时，需要依赖另一个对象，这是代码层中的一种“依赖”关系；
当在代码中声明了依赖关系后，Angular通过injector注入器将所依赖的对象进行注入操作

在Angular中，每一个Angular应用都有一个injector注入器来处理依赖的创建，注入器实际上
是一个负责查找和创建依赖的服务定位器，所以声明的依赖注入对象都是由它来进行处理，
此外，当获取injector注入器对象后，还可以调用该对象的get函数来获得任何一个已经被定义
过的服务的实例