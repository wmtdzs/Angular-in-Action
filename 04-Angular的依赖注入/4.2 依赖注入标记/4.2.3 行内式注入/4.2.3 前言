无论是推断式，还是标记式注入，代码都显得冗余，且还反复使用了临时变量。
为了避免这些不足，Angular允许开发人员使用行内注入声明的方式来注入服务。
所谓行内注入指的是在构建一个Angular对象(如控制器对象)时，允许开发人员将
一个字符型数组作为对象的参数，而不仅仅是一个函数，在这个数组中，除了最后
一个必须是函数体外，其余都代表注入对象中的服务名，而它们的名称和顺序与最后
一个函数的参数是一一对应的。