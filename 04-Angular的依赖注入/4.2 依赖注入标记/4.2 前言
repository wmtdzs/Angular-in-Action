每个Angular应用都是由注入器(injector)负责查找和创建依赖注入的各种服务，
而这个注入器本质上来说就是一个服务的定位器，它可以快速的定位到应用中
需要注入的各种服务，而在服务定位过程中，需要应用提供一些诸如时依赖的标记，
通过这些标记，告诉注入器需要注入什么样的依赖服务，而这些标记，就是依赖
注入标记。

根据依赖注入标记声明的方式不同，可以将它们分为3种形式
推断式注入，标记式注入和行内注入