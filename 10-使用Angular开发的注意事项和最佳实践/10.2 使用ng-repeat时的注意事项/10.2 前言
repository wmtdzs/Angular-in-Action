在Angular中，ng-repeat是一个非常重要的内部指令，它的功能是在遍历数组过程中生成DOM元素，
实现在页面中展示列表数据中的功能，若使用不当，会出现一些问题，具体如下：
1. 若有过滤器，调用$index并不能准确定位到对应的记录
2. 在调用ng-repeat指令重新请求数据时，并不是在原来DOM元素中更新数据，而是再次新建DOM元素。
3. 在通过ng-repeat指令生成的子元素中，若通过父元素的scope对象更新数据时，不能直接更新
遍历的数组源，而必须进行逐个更新。