在Angular中，ng-if指令的功能与ng-show指令相似，都用于控制元素的显示与隐藏，但两者又有区别，
ng-if指令会移除DOM原有的元素，而ng-show指令只是将元素的display属性设置为none，因此，在使用
时必须根据实际的需要进行选择性适用。
此外，与其他指令一样，ng-if指令也会创建一个子级作用域，因此，若在ng-if指令中添加了元素，并
向元素属性增加ng-model指令，那么ng-model指令对应的作用域属于子级作用域，而并非控制器注入的
$scope作用域对象，这点在进行双向绑定时，需要注意。
