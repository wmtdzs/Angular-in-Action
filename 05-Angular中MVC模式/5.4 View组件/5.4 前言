在Angular的MVC模式下，V指的就是视图层，即View组件，但这种组件并不是普通的
视图模板元素，准确来说，它是先经过浏览器加载并渲染后，再根据视图模板和对应
控制器模型修改后所包含的DOM元素。

View组件是多方作用下的结果，这也使得它的最终展示并不是独立的，它的外形取决于
视图模板，内部数据来源于控制器，所以，可以通过ng-view指令加载和切换视图模板，
并将视图组件通过ng-controller指令与控制器相绑定