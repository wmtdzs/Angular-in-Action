在Angular中，创建好的服务就是一项数据请求和处理的集合，在后续开发中要修改，
相对来说是非常困难的，特别是一些内置的服务，修改这些服务可能要改动整体Angular
框架文件，面临风险较高。
针对上述问题，在Angular中，专门为服务添加了一些设置项，如装饰器(decorator)，
通过它可以在不修改原有服务的情况下，添加一些其他的功能。此外，服务对象在调用
过程中，是单例的，也可以通过修改代码的方式返回非单例的服务对象。